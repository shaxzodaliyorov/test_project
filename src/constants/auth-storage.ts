/** Raw JWT access token only */
export const MOCK_ACCESS_TOKEN = 'mock_access_token'

/** Legacy JSON { token, user } — migrated to MOCK_ACCESS_TOKEN */
export const MOCK_SESSION_KEY = 'mock_session'

/** Legacy zustand persist key */
export const LEGACY_AUTH_STORAGE_KEY = 'auth-storage'
